fn say(say_a, msg_a, msg_b) 
{
	var msg = if (say_a) { msg_a } else { msg_b };
	print(msg);
};

fn do_it
{
	var a = "HELLO";
	var b = "BYE";
	
	say(0, a, b);
	say(1, a, b);
};

do_it();