fn puts(m) {
	print(m);
	print("\n");
};

fn say(say_a, msg_a, msg_b) 
{
	var msg = if (say_a) { msg_a } else { msg_b };
	puts(msg);
};

fn do_it(say_fn)
{
	var a = "HELLO";
	var b = "BYE";
	
	say_fn(0, a, b);
	say_fn(1, a, b);
	
	"DONE";
};

print(do_it(say));